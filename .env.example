# Base URL for tests
BASE_URL=https://example.com

# Test user credentials (for demo purposes only - never commit real credentials)
TEST_USERNAME=testuser
TEST_PASSWORD=testpassword

# Test environment
TEST_ENV=staging

# Timeouts (in milliseconds)
ACTION_TIMEOUT=10000
NAVIGATION_TIMEOUT=30000

# Browser settings
HEADLESS=true
SLOW_MO=0

# CI/CD settings
CI=false
